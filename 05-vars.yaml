- name: variables
  hosts: web
  connection: local
  vars: #play level variables
    course: "DEVOPS with AWS"
    trainer: siva
    duration: 120HRS
  tasks:
  - name: print variables
    ansible.builtin.debug:
      mgs: "course name is "